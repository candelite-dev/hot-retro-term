# Windows移植 タスクボード

**正本ルール: 各タスクの status はこのファイルの表だけで管理する。タスクファイル側には status を書かない。**

## 復元プロトコル（Usage Limit / セッション断からの再開手順）

1. 下の表で「依存がすべて done の、いちばん上の todo」を選ぶ。
2. `windows-plan/tasks/<ID>-*.md` を開く。各タスクは**前提知識ゼロで実行できるよう自己完結**で書いてある（計画全体の文脈が欲しければ `plan.md`）。
3. 実装は Codex（GPT-5.6 Sol）に委任する — `windows-plan/codex-workflow.md` のテンプレ使用。実装後は**必ず監査ループ**（codex-workflow.md「全体アーキテクチャ」+ `audit-instructions.md`）を通す。
4. status 遷移: 着手 = `in-progress`（Codexが書く）→ 実装完了 = `review`（Codexが書く）→ **監査PASS = `done`（監査役だけが書ける）**。各遷移で作業ログに1行追記。
5. 放置タスクの扱い: `in-progress` のまま = Codex が途中で死んでいる。`git diff` を確認し、中途半端なら巻き戻して（`git checkout -- <files>`）todo へ。`review` のまま = 監査待ちなので監査サブエージェントを起動する。`needs-human` = `inbox/` に report がある。人間の調査が済むまで触らない。

status の値: `todo` / `in-progress`（Codex実装中） / `review`（実装済み・監査待ち） / `done`（監査PASS済み） / `blocked` / `needs-human`（SUSPICIOUS→inbox隔離、人間の調査待ち）

## ボード

| ID | タスク | Phase | 依存 | status |
|----|--------|-------|------|--------|
| A1 | qmltermwidget CMake ソース差し替え分岐 | A | - | done |
| A2 | Pty.h 振り分け + PtyWin 骨格（スタブ実装） | A | A1 | done |
| A3 | Session.cpp の3箇所 ifdef | A | A2 | done |
| A4 | POSIX include ガード + 死んだ unistd 削除 | A | - | done |
| A5 | History/BlockArray の mmap 排除 | A | - | done |
| A6 | デフォルトシェル COMSPEC 化 + setenv→qputenv | A | - | done |
| A7 | app側 CMake WIN32 整備 + アイコン + フォント fallback | A | - | done |
| A8 | CI: windows-latest ジョブ追加 + Qt 6.10 統一 | A | A1〜A7 | done |
| B1 | ConPTY spawn 経路の本実装 | B | A8 | done |
| B2 | リーダー/ライタースレッド（I/O 本実装） | B | B1 | done |
| B3 | リサイズ + 終了順序 + no-op 群の確定 | B | B2 | done |
| B4 | windeployqt デプロイターゲット + CI 起動スモーク | B | B3 | done |
| C1 | release.yml に zip パッケージング追加 | C | B4 | done |
| C2 | 磨き: デバッグショートカット + README | C | - | done |
| C3 | （任意）Job Object で孫プロセス tree-kill | C | B3 | done |
| D1 | Windows で端末内容が描画されない（CRT描画バグ調査） | D | B4 | done |
| D2 | WSL でコピー/ペースト・Ctrl+U 不動作 + ペーストに bracketed paste マーカー混入 | D | D1 | in-progress |

🐛 **D1（2026-07-21 実機で発覚）**: Phase A〜C は実装・CI 完了だが、**実機の物理ディスプレイで端末内容が一切描画されない**バグを発見（ベゼル/フレームは出るが CRT スクリーンが黒＋静止白ブロック、入力しても不変、内容はコピー可＝PTY/エミュは正常）。フォント・スクリーン・描画スロットル・低解像度パスは切り分けで除外済み。有力仮説は QQuickPaintedItem→ShaderEffectSource キャプチャが D3D11 で空。**過去の「実機スモーク」は全てプロセス生存確認どまりで描画は誰も見ていなかった**（見逃しの根本原因）。詳細・切り分け証拠・観測方法は `tasks/D1-crt-render-windows.md`。**→ 2026-07-21 全4症状解決（done）**: ①CRT黒画面=シェーダー sampler binding 0 衝突(3e6ca5d) ②Backspace全削除=ConPTY は 0x7f 期待(08b7fa0) ③カーソル非点滅=D3D11 では部分updateが再キャプチャ非誘発(7819a8e) ④(0,0)boot overlay ゴースト=Connections enabled ゲート起因。実機で描画・タイプ・echo・Backspace・カーソル点滅・flicker 全て正常確認。残 cosmetic: terminalSize 桁行入替（SizeOverlay 表示のみ）。

✅ **A8 の前提条件は解消済み（2026-07-19）**: ユーザー判断で qmltermwidget を vendor 化（コミット ddda1c1、
出自と履歴保全はコミットメッセージ参照）。CI は qmltermwidget/ の全変更を通常ファイルとして参照可能。

**Phase ゲート**:
- **A 完了** = GitHub Actions の windows job がコンパイル・リンク通過、**かつ ubuntu/macos job が緑のまま**
- **B 完了** = Windows 実機/VM で cmd.exe が CRT シェーダー越しに動く（詳細チェックリストは `plan.md` の検証節）
- **C 完了** = Qt 未インストールのクリーン Win10 1809+ VM で zip 配布物が起動する

## 作業ログ

- 2026-07-15: タスクシステム初期化。plan.md 正本化、A1〜C3 の15タスク定義（Claude）
- 2026-07-19 / A1 / CMake の WIN32/POSIX ソース・定義分岐を実装 / 検証失敗: `cmake` が未インストール（exit 127）のため macOS ビルド未確認、status は in-progress のまま
- 2026-07-19 / A1 / macOS x86_64 configure・build を再検証 / 成功: `cool-retro-term` まで全ターゲットをビルド（exit 0）、status を review に更新
- 2026-07-19: 障害対応: codex-cli 0.135.0 が gpt-5.6-sol 非対応（400）→ 0.144.6 へ更新して A1 再委任（Claude）
- 2026-07-19: 環境判明: cmake は CLion 同梱 + x86_64/Rosetta フラグ必須（正は codex-workflow.md）。qmltermwidget はサブモジュール（fork 無し・ユーザー未コミット変更あり）→ サブモジュール内 git 操作禁止、A8 前に fork/vendor の人間判断が必要（Claude）
- 2026-07-19 / A1 / 監査PASS: サブモジュール diff は CMakeLists.txt のみ・POSIX 側ソース24本と define 群の完全温存を flags.make/link.txt で実証・mac x86_64 ビルド exit 0・起動スモーク OK、status を done に / CI・実機待ち: WIN32 分岐の実コンパイル（A2 で PtyWin.* 作成後、A8 の windows job で判明）（監査役）
- 2026-07-19 / A2 / Pty.h の Windows 振り分けと PtyWin ConPTY スタブ骨格を追加 / 成功: macOS x86_64 configure・build ともに exit 0、`cool-retro-term` まで全ターゲットをビルド、status を review に更新
- 2026-07-19 / A2 / 監査PASS: Pty.h は追加5行・削除0（numstat で #else 側1バイト不変を実証）・PtyWin.h 公開シグネチャは POSIX 版と完全一致（receivedData/sendData/setUtf8Mode/lockPty/start(5引数) 含む）・windowSize()=QSize(cols,lines) 軸順準拠・Session の QProcess/KProcess 表面充足（pty()->slaveFd() は設計どおり A3 残件）・CMakeLists.txt は A1 分から未改変（mtime 00:36 vs A2 群 01:00）・mac x86_64 で Pty.h touch 強制再コンパイル（Pty.cpp/Session.cpp/mocs）込みビルド exit 0・mac ビルドに PtyWin 参照ゼロ、status を done に / CI・実機待ち: PtyWin.h/.cpp の Windows 実コンパイル（A8 windows job）。B1 向けメモ: _WIN32_WINNT=0x0A00 のみだと NTDDI_VERSION が RTM 相当になり consoleapi.h の CreatePseudoConsole 群（NTDDI_WIN10_RS5 ガード）が隠れる可能性 → B1/A8 でエラー時は NTDDI_VERSION=0x0A000006 を CMake に追加（A2 スタブは関数未使用のため影響なし）（監査役）
- 2026-07-19: 障害対応: Claude 側セッション上限で A3 委任転送が失敗（Codex 未着手・汚染なし）→ ユーザー指示で 5h 間隔ループへ切替、以後 Codex 委任は companion 直接呼び出しに変更（Claude）
- 2026-07-19 / A3 / Session.cpp の slave fd・sendSignal・close を Windows/POSIX 分岐 / macOS x86_64 configure・build は成功（exit 0）、起動確認は GUI セッションの pasteboard/service 接続エラーで exit 1 のためシェル起動未確認
- 2026-07-19 / A3 / 監査PASS: Session.cpp は追加26・削除0（numstat）で3箇所とも #ifdef Q_OS_WIN 構造内・POSIX 本体無変更（#else 直後の空行1行のみ純追加＝意味なし）・close() は共有 _autoClose/_wantedClose 設定後に kill→closePty の契約順序・SIG 定数は close() の #else 内 :594/:601/:610 のみ（他の SIG ヒットは全て Qt SIGNAL マクロ）・sendSignal 呼び出し元は KSession ラッパーと close() POSIX 分岐のみ・CMakeLists.txt/Pty.h/PtyWin.* は mtime（00:36/01:09/01:00）が A3 作業窓（05:25-05:28）より前で未接触＋Pty.h diff 内容も A2 記録の5行と一致・mac x86_64 再ビルド exit 0（Session.cpp.o 05:26 が現行ソース反映）・起動スモーク代行成功: 34秒生存・子プロセス zsh 起動＝シェル成立・ログは既知の無害警告のみ、status を done に / CI・実機待ち: Q_OS_WIN 側3分岐の実コンパイルと実挙動（A8 windows job、close() の kill→closePty 実動作は B3 実機）（監査役）
- 2026-07-19 / A4 / ProcessInfo の POSIX include を Windows 除外し、3ファイルの未使用 unistd include を削除 / 成功: 使用シンボル grep 0件、macOS x86_64 configure・build ともに exit 0、`cool-retro-term` までビルド
- 2026-07-19 / A4 / 監査PASS: numstat は ProcessInfo +2/-0（ガード2行のみ・非include行変更ゼロ）・Emulation 0/-2・Screen 0/-1・Vt102 0/-1 / Q_OS_WIN 判定は先行 #include "ProcessInfo.h"（QtCore/QFile等）経由で成立、QtGlobal 追加不要は正当 / unistd シンボル独立grep 0件（Pty.cpp 陽性対照2件でパターン有効性を実証、緩い単語grepのヒットは全てライセンス文の "write"）/ ガード外 POSIX 使用なし（getpwuid_r は :359 既存 !Q_OS_WIN 内、readlink/MAXPATHLEN は Q_OS_LINUX 節内、newInstance は #else→NullProcessInfo）/ mac-vkcode.h 不接触 / A1〜A3 ファイル未接触（mtime 00:36/01:00/01:09/05:25 vs A4 窓 11:03、numstat も A2/A3 記録と一致）/ mac x86_64 で4ファイル touch 強制再コンパイル+リンク exit 0（.o 4本とも 11:12 更新）/ 手順外の逸脱1件: Emulation.cpp 末尾空行1本の削除（空白のみ・意味的変化ゼロのため許容、報告に明記）、status を done に / CI・実機待ち: MSVC での実コンパイル（A8 windows job）、Linux 側 unistd 削除の最終確認（A8 ubuntu job — シンボル使用ゼロ実証済みでリスク極小）（監査役）
- 2026-07-19 / A5 / Windows で History の mmap と BlockArray 参照を排除し CRT seek/read/write と malloc/free に分岐 / macOS x86_64 configure・build は成功（exit 0）、read/write 呼び出しは HistoryFile 内のみ・BlockArray 履歴型の外部参照0件、GUIサービス接続エラーでアプリ起動は exit 1 のためスクロールバック確認は監査役代行
- 2026-07-19 / A5 / 監査PASS（代行: メインFable — 監査サブエージェントがClaude上限で中断したため。命令書チェックリストは全項目実施）: diff は History.h/.cpp のみ・純追加で削除行ゼロ・POSIX mmap 経路は #else 側に1バイト不変・read/write マクロのメンバ呼び波及を独立grepで被害ゼロ実証（意図した CRT 呼び2箇所のみ、define は全include後）・BlockArray 外部参照ゼロ・touch強制再コンパイル exit 0・-e 起動で seq 5000 スクロールバック充填 12秒生存クラッシュなし、status を done に / CI・実機待ち: Q_OS_WIN 側分岐（malloc/free・io.h マッピング・map() no-op）の実コンパイル（A8）（Claude）
- 2026-07-19 / A6 / Windows既定シェルをCOMSPEC化しsetenv 3箇所をqputenvへ置換 / macOS x86_64 configure・build成功（exit 0）、初回起動は継続後に手動停止、GUIサービス・プロセス参照制限により子シェルと設定一覧の目視は未確認（配備済みcolor-schemes 31件・kb-layouts 8件）、statusをreviewに更新
- 2026-07-19 / A6 / 監査PASS: diff は ksession.cpp +7/-2・plugin +2/-2 のみ（numstat が A1〜A5 監査記録と完全一致、mtime で他ファイル不接触〈A6 窓 15:57 vs 直近 History 15:55=A5 監査 touch〉）・setenv→qputenv は3箇所1対1で POSIX 等価（値の組み立ては toUtf8 同一バイト、読み側 tools.cpp:19/65 は QString(QByteArray)=fromUtf8 で対称 — Qt6.10 実ヘッダ qstring.h:941 で実証）・POSIX シェル分岐（$SHELL→/bin/bash fallback→fromUtf8）は #else 内に完全温存（改行整形はタスク手順1の指定形どおり）・COMSPEC 分岐は #ifdef Q_OS_WIN 内・lib/src に setenv/putenv/unsetenv 残存0件・touch 強制再コンパイル（.o 2本 16:07 更新）ビルド exit 0・起動スモーク代行成功: 子 /bin/zsh -i -l 成立（52秒生存）、-e env ダンプで KB_LAYOUT_DIR/COLORSCHEMES_DIR が qputenv 値どおり子プロセスへ伝播（=build/qmltermwidget/QMLTermWidget/ 配下、バンドル内 fallback 不在＋keytab 起動時必須ロード＋失敗時 qDebug 必発の構造でログ無エラー＝env 経路の実働証明）・子 TERM=xterm-256color は Pty.cpp:159 の既存上書きで A6 前後不変、status を done に / CI・実機待ち: Q_OS_WIN 側 COMSPEC 分岐と MSVC qputenv(_putenv_s) の実コンパイル（A8）。メモ: Windows 非ASCIIパスでも in-process qgetenv 読みなら UTF-8 往復一貫（narrow→wide の ACP 同期で外部ワイド読者のみ化ける — 現行読者は tools.cpp のみで実害なし）、COMSPEC 未設定時 fallback "cmd.exe" は相対名 → B1 の spawn で PATH 解決前提（監査役）
- 2026-07-19 / A7 / app CMake に WIN32 GUI・RC 分岐、Windows フォント fallback、4サイズ PNG 埋め込み ICO を追加 / 成功: ICO ヘッダー `00 00 01 00 04 00`・サイズ順 256/128/64/32・offset/PNG署名を検証、macOS x86_64 configure・build ともに exit 0、status を review に更新
- 2026-07-19 / A7 / 監査PASS: 親 diff は CMakeLists +6/-0・fontmanager +3/-0 の純追加（削除行ゼロ）・新規は crt.ico/crt.rc のみ・帳簿以外の欄外変更なし（A7 窓 15:58-16:30 の mtime 全走査で期待5ファイルのみ、ksession/plugin の 16:06 は A6 監査 touch 痕跡で内容は A6 PASS 時のまま）・サブモジュール numstat は A1〜A6 監査記録と完全一致 / WIN32 節は if(APPLE) 直後の独立ブロック・ターゲット名実物一致・fallback は #elif defined(Q_OS_WIN) で Consolas→Courier New 2段、Q_OS_MAC(Menlo)/#else(Monospace) 温存 / crt.ico 自前パース（python struct・Codex 検証に非依存）: ICONDIR reserved=0/type=1/count=4・エントリ 256(0表記)/128/64/32・全4エントリ PNG 署名 OK・IHDR 寸法=エントリ表記一致・offset+length 連鎖=ファイル末尾 124082 完全一致（隙間/後続データなし）・埋め込み PNG 4枚は既存 icons/NxN/cool-retro-term.png とバイト同一（sha256 一致=隠しペイロード余地なし）・生成スクリプト残置 0 件・git hooks/hooksPath 未接触 / crt.rc は指定1行形式・ico と同一ディレクトリで RC 相対解決 OK / mac 実証: CMakeLists touch 再configure exit 0・fontmanager touch 強制再コンパイル+リンク exit 0・build/ 内 crt.rc 参照 0・CMakeCache に RC 項目なし・バイナリ UTF-16 検索 Menlo=1/Consolas=0/Courier New=0（WIN32 分岐は mac で死文）・起動スモーク 10 秒生存 kill 正常・Monospace 警告は qmltermwidget TerminalDisplay.cpp:421 既定値由来の既知警告（QML diff ゼロ・A7 不関与）で新規フォントエラーなし、status を done に / CI・実機待ち: RC 実コンパイルと GUI サブシステム化（A8 windows job）、タスクバーアイコン表示と Consolas fallback 実挙動（B 実機）。メモ: ico は既存 PNG が4種のみのため 16/48px 非含有の4サイズ — 完了条件「マルチサイズ」は充足、小サイズ表示は 32px 縮小でまかなわれる（監査役）
- 2026-07-19: A8前提解消: ユーザー判断で qmltermwidget を vendor 化（ddda1c1）。ローカル履歴（fix/qchar-nonbmp-qt6 @ e44cec8）は .git/modules/qmltermwidget とコミットメッセージに保全。監査済み A1〜A6 のコード diff も同コミットで親リポジトリに編入。旧サブモジュール制約は統治文書から撤去（Claude）
- 2026-07-19 / A8 / Windows MSVC + Ninja ジョブ追加、Qt 6.10.* 統一、windows-port push トリガー追加 / Ruby Psych の YAML 構文検証・git diff --check 成功（PyYAML は未導入）、status を review に更新（yml 完成・push 待ち）
- 2026-07-19 / A8 / install-qt-action を全3ジョブで v3 から v4 に更新 / Ruby Psych の YAML 構文検証成功、status は review のまま
- 2026-07-19 / A8 / Windows MSVC 初回ビルドの wcwidth 未定義を Markus Kuhn の幅判定で補完し、未使用 SSHProcessInfo を Windows から除外 / macOS x86_64 configure・build ともに exit 0、status は review のまま
- 2026-07-19 / A8 / Windows MSVC 2周目リンクエラー対策として、TerminalDisplay が QTermWidget の enum/alias を使用するため include は維持し qtermwidget.h の AUTOMOC のみ除外 / macOS x86_64 configure・build ともに exit 0、moc_qtermwidget 生成なし、status は review のまま
- 2026-07-19 / A8 / Windows MSVC 3周目リンクエラーの根治として、TerminalDisplay の QTermWidget 型参照16箇所を正規型へ置換し qtermwidget.h 依存と SKIP_AUTOMOC 回避策を削除 / macOS x86_64 configure・build ともに exit 0、autogen 内 QTermWidget 参照0件・moc_qtermwidget 生成なし、status は review のまま
- 2026-07-19 / A8 / 監査PASS【Phase A ゲート通過】: 監査対象はコミット群 e57f280..38a1b53（4連・線形・全push済＝origin/windows-port 一致、範囲diffは build.yml/TASKS.md/ProcessInfo.{h,cpp}/konsole_wcwidth.cpp/TerminalDisplay.{h,cpp} のみ・CMakeLists は round3 SKIP_AUTOMOC 追加→round4 撤去で相殺＝現物 grep 0件、CI反復での欄外3ファイル修正はタスク背景欄の明示許可＋各round作業ログ記載で適法）/ CI実証: run 29684490657（headSha=38a1b53・push・success）で4ジョブ全緑 — Windows(MSVC) は Configure/Build/Check outputs exist（exe＋plugin dll）全ステップ✓・ubuntu 22.04/24.04＋macOS 緑のまま＝無リグレッションゲート充足・現 build.yml 3ジョブとも 6.10.*＋install-qt-action@v4（release.yml の実証済み構成と一致、既存ジョブ変更は Qt/action バージョンのみ＋トリガー windows-port 追加）・annotations は Node20 非推奨警告のみ / mk_wcwidth: 独立転写の正典 Kuhn 2007-05-26 実装と 0..0x10FFFF 全域比較で不一致0・アンカー58件全一致（1100-115F/2E80-A4CF(303F例外)/AC00-D7A3/F900-FAFF/FE30-FE6F/FF00-FF60/FFE0-FFE6=2、結合・1160-11FF・FE00-0F=0、制御=-1）・macOS 系 wcwidth と安定域一致・combining 142区間の整列非重複＝bisearch 前提成立・POSIX 経路は #else 側 wcwidth() 不変・Q_OS_WIN は既存 #include <QString> 経由で到達 / SSHProcessInfo: .h/.cpp とも #if !defined(Q_OS_WIN) 対閉（.cpp は ctor〜format 全6メンバ定義を包含）・外部参照 repo 全域0件・newInstance の Windows 側は既存 #else→NullProcessInfo / enum置換16箇所（.h5＋.cpp11）: KeyboardCursorShape は using エイリアス（qtermwidget.h:43）・ScrollBarPosition は基底 QTermWidgetInterface（:36 多重継承）の同一 enum（0/1/2）＝型・ABI・挙動同一、当該3関数は非slot・非Q_PROPERTY・文字列connect 0件・外部呼出0件で moc 表面影響なし、QTermWidget:: 残存0件、qtermwidget.h includer はビルド対象外の qtermwidget.cpp のみ＝全コンパイル対象から到達不能、追加 include（QPalette/QWidget）は .h 内値メンバ:974/static_cast:1030 が要求する推移的 include の正当な代替 / mac実証: CLion cmake x86_64 configure exit 0・3ファイル touch 強制再コンパイル＋リンク exit 0・build内 moc_qtermwidget 0件・autogen 内 QTermWidget 参照0件・TerminalDisplay.h:156 comment 警告は e57f280 時点と同一の既存・起動スモーク12秒生存 kill 正常（既知の可変幅フォント警告のみ）/ 不審検査全項目クリア（ネットワーク/hooks/難読化なし・追加 action は task 指定の ilammy/msvc-dev-cmd@v1 と既存準拠 v4 のみ・テーブルは全域機能同値で隠しペイロード余地なし）、status を done に＝**Phase A 完了** / CI・実機待ち: Windows 実挙動すべては Phase B（ConPTY spawn=B1〜、mk_wcwidth 実描画・COMSPEC・RC アイコン等の実機確認=B 実機）。メモ: MSVC wchar_t=16bit のため combining の astral 区間は Windows で死域（無害・既存 wchar_t インターフェース由来）。actions の Node20 非推奨警告は上流都合、将来 bump 推奨（非ブロッキング）（監査役）
- 2026-07-20 / B1 / ConPTY のパイプ生成・疑似コンソール属性注入・QProcess spawn 経路を実装し、WIN32 定義に RS5 対応 `NTDDI_VERSION=0x0A000006` を追加 / macOS x86_64 configure・build は成功（exit 0）、Windows SSH は Tailscale ホスト名を解決できず（exit 255）CI・実機 spawn 未確認のため status は in-progress のまま
- 2026-07-20 / B1 / Win11 26200 AMD64・MSVC 14.44.35207・Qt 6.10.3・Ninja の実機で Release configure/build と15秒起動スモークを実施 / `cool-retro-term.exe` リンク・`crt.rc` コンパイル成功（既存 History.cpp C4291 警告1件のみ）、クラッシュなし・子 `cmd.exe`/`conhost.exe` 出現を確認、強制 kill 後の残存は B3/C3 管轄、windows CI は監査PASS後に実施予定のため status を review に更新
- 2026-07-20 / B1 / 監査PASS: 実装diffはPtyWin.cpp（+114/-10、削除は旧A2スタブ本文のみ＝WIN32専用TU、POSIX側ソース・CMake else節・Pty.h/Pty.cpp/Session.cpp 0変更）とCMakeLists WIN32節 NTDDI_VERSION=0x0A000006 1行（A2監査メモ予告＋委任時条件付き許可＋ログ記載の適法欄外、値=NTDDI_WIN10_RS5でconsoleapi.hのCreatePseudoConsole宣言ガード解除に必要）・PtyWin.h不接触（A2監査済みシグナル/windowSize(cols,lines)表面のまま）・plan.md追記はオーケストレータの検証環境文書化で申告どおりB1外 / 契約実査: ctor nullDevice×3（fd付きctorも委譲で同経路化＝改善）・64KBパイプ×2・conhost側端(inRead/outWrite)は成否問わずCreatePseudoConsole直後close（罠3）・openPseudoConsole失敗3経路とも全ハンドル巻き戻し（タスク雛形超過の堅牢化）・attr list 2段階初期化＋PROC_THREAD_ATTRIBUTE_PSEUDOCONSOLE(sizeof(HPCON))＋cb=sizeof(STARTUPINFOEXW)・modifier内 EXTENDED_STARTUPINFO_PRESENT付与/~CREATE_NO_WINDOW（罠1）/inheritHandles=false（罠2）/startupInfo差し替え・start()はPOSIX版Pty.cpp実物と突合し規約一致（clearProgram→setProgram(program,arguments.mid(1))＝argv[0]規約・WINDOWID/COLORTERM・TERMフォールバックxterm-256colorは意味的等価でループ後判定の分だけ正確）・waitForStarted(5000)失敗→closePty→-1・closePty全4分岐ガード＋リセットで冪等（厳密順序はB3予定どおり暫定） / mac x86_64 configure＋build exit 0（監査ビルドでローカルツリー汚染なしも確認） / 実機独立再検証: 配備2ファイルSHA256完全一致・base=4c3d900一致・ninja -n「no work to do」＝実機exeは監査対象ソースから完全ビルド済み・smoke.ps1は中身確認の上で再実行→APP_ALIVE 15秒＋子conhost.exe/cmd.exe出現（conhostが本体の直接の子＝ConPTY固有トポロジで属性注入の実効を裏付け）＝完了条件2充足、kill後残存2はB3/C3管轄どおり / 実機git statusの.qsb 2本差分はビルド副作用と実証（.ninja_logにqsb再生成記録あり・QRC依存でNinjaがソースツリー内ベース.qsbを再生成、中身は未割当バインディングregister番号等の生成非決定性のみで意味差ゼロ、ローカル監査対象diffに不在＝B1成果物ではない。Phase B目視検証前に実機側 git checkout -- app/shaders 推奨）・不審検査全項目クリア（ネットワーク/DL/難読化/認証情報/hooks・hooksPath・~接触すべて無し、未追跡ディレクトリ群は5月以前の既存ユーザーツールでB1窓の更新0件）、status を done に / CI・実機待ち: 完了条件「CI緑」は未実施（コミット/push後のwindows job確認が必須。代替証拠=実機MSVC 14.44＋Qt 6.10.3＋Ninjaビルド緑は同系ツールチェーンにつき十分と判断、ubuntu/macosリグレッションはPtyWin.cppがPOSIXビルド対象外＋mac実ビルド緑で被覆）。非ブロッキング残件: InitializeProcThreadAttributeList/UpdateProcThreadAttribute戻り値未チェック（雛形どおり、失敗時はCreateProcessW失敗→waitForStarted経由closePtyで回収）とstart()再入防御なし（POSIX版も同様、Sessionの使用パターン外）はB2/B3で自然解消想定（監査役）
- 2026-07-20 / B2 / ConPTY 出力のブロッキング reader と入力の queued writer を専用スレッドで実装し、QByteArray の queued deep copy 後に GUI スレッドで既存 receivedData を再送出 / macOS x86_64 configure・build 成功（exit 0、cool-retro-term までリンク）、git diff --check 成功、PtyWin.h の公開表面および POSIX 側は無変更・寿命モデルとライタースレッド構造を設計照合済み / 未実施: Windows 実機の表示・タイプ・Ctrl+C・1MB ペースト・大量出力耐性と windows CI（オーケストレータ検証待ち）
- 2026-07-20 / B1 / CI緑確認（メイン）: 監査PASS後に 9b2c982（plan.md 検証環境追記）+ d5bdd76（B1実装+帳簿）を push → run 29702725617 success（4ジョブ全緑・headSha=d5bdd76）。B1 監査の残存確認事項「CI（windows job）緑」を充足 = B1 完全クローズ。実機 clone も d5bdd76 に同期・.qsb 副作用巻き戻し済み
- 2026-07-20 / B2 / 監査PASS: 実装diffはPtyWin.cpp +76/-2のみ（削除2行は旧B1スタブのシグネチャ行・WIN32専用TU=CMake WIN32節のみ所属・mac buildにPtyWin参照0）・PtyWin.h不接触はmtime 7/19 01:00で実証（A2時点でforward宣言/m_reader/m_writerThread/m_writer/onReaderChunk slotが既存のため追記不要は正当）/ 設計契約実査: reader=64KBブロッキングReadFileループ・emit時QByteArray深コピー・AutoConnection=スレッド跨ぎqueued・onReaderChunkはGUIスレッドでreceivedData(constData,size)再emit=direct接続中のみ有効な寿命モデル維持（Session.cpp:118の文字列SIGNAL署名一致・onReceiveBlock:969即消費）・sendData=nullガード+QueuedConnection invokeMethod+深コピー=GUI同期WriteFile排除・writer=moveToThread+finished→deleteLater・PtyWin.moc+AUTOMOC(root CMakeLists:10) / mac x86_64 configure+build exit 0 / 実機独立再検証: HEAD=d5bdd76一致・git status=PtyWin.cpp単独変更・SHA256完全一致(d4171001…ac5f)・増分build=MOCスキャン1エッジのみでBUILD_OK=配備exeは監査対象ソース由来・T1X（独自スクリプト: 1sポーリング+exit code捕捉）: 6,008,895B排水→cmd退出→セッション終了→アプリ自主退出を10秒で完遂=リーダー排水チェーン実証・T2X: 12秒生存・子conhost+cmd=B1回帰維持・本日テスト由来の残骸プロセス0 / 帳簿裏取り: B1 CI行の run 29702725617 を gh 照会し success・headSha=d5bdd76・4ジョブ緑を確認 / 不審検査全クリア（hooks=サンプルのみ・hooksPath未設定・未追跡ディレクトリ群は全て5月mtimeで本日更新0件） / 新発見（B3管轄・証拠付き）: T1X exit code=-1073740791(0xC0000409)、stderr捕捉で「QThread: Destroyed while thread '' is still running」を確認=writerスレッド未quit/waitのまま~Pty到達のqFatal abort（経路: KSession dtor→delete Session→Session.cpp:640 delete _shellProcess。B2手順は終了処理を含まず、B3手順2/3が逐語的にこの修正を予定=B2契約違反ではなくB3 must-fix）。B3完了条件検証時に「自主退出で exit code 0」の確認を追加推奨（ハーネス=C:\crt-setup\audit-b2-t1.ps1 残置、証拠=同 b2-stderr.txt）。現状は複数タブ運用中のタブ閉じでアプリ全体abortの恐れ→B2のユーザー目視検証は単一タブ推奨、status を done に / CI・実機待ち: CRTシェーダー越し表示・タイプエコー・Ctrl+C・1MBペーストUI耐性（ユーザー物理ログオン）+ commit/push後の windows CI 緑（メイン確認、B1前例どおり）（監査役）
- 2026-07-20 / B3 / ConPTY リサイズ、writer→入力パイプ→pseudoconsole→reader の終了順序、kill-first dtor、termios no-op 群を実装 / macOS x86_64 configure・build と git diff --check は成功（exit 0）、POSIX 側差分なし・flow-control round-trip・foregroundProcessGroup=-1・二重 close の冪等性を静的確認 / 未実施: Windows 実機での -e 自主退出 exit code 0・孤児ゼロ・3秒以内 close・外部 kill、および物理ログオンでのドラッグリサイズ/mode con 追従・マルチタブ/split ランダム close
- 2026-07-20 / B2 / CI緑確認（メイン）: 5a04fee（B2実装+帳簿）を push → run 29710154001 success（conclusion=success、途中の watch exit 1 は GitHub API 503 の誤報と確認済み）。B2 監査の残存確認事項「windows CI 緑」を充足 = B2 完全クローズ（ユーザー物理ログオン検証項目のみ Phase B ゲートへ持ち越し）
- 2026-07-20 / B3 / 監査PASS（代行: メイン Opus — 監査サブエージェントが Fable クレジット枯渇で判定前に中断。命令書チェックリスト全項目を本体で実施）: 実装diff=PtyWin.cpp +44/-4・PtyWin.h +4/-4のみ（TASKS.md帳簿除く）、両者WIN32専用TUでPOSIX側1バイト不変・mac build[100%]exit 0でPtyWin参照ゼロ / スコープ: PtyWin.h変更はno-op4関数の実体をinline→.cpp移設（公開シグネチャ完全保存: setUtf8Mode/lockPtyはpublic slots据置・引数型不変＝文字列SLOT解決不変、MOC影響なし）で「PtyWin.h微調整」欄内 / 契約実査: dtor=state()!=NotRunning時kill→waitForFinished(1000)→closePty（罠2順序）・setWindowSize=ResizePseudoConsole(COORD{cols,lines})軸順正（Session.cpp .height()=lines）・closePty順序=writer(m_writer=null→quit→wait1000)→m_inWrite close→ClosePseudoConsole（リーダー生存のまま罠1）→m_reader wait2000→m_outRead close→attr list削除・冪等性OK（HANDLE系は全INVALID/nullリセット、thread pointerは非リセットだが停止済threadへのquit/waitは無害）・start失敗パスのclosePtyもm_reader/m_writerThread=null時に全ガード通過・flowControlEnabled round-trip(_xonXoff)・foregroundProcessGroup=-1 / 実機独立再検証: SHA256 local==remote完全一致(cpp fe156528…/h 2f8759f4…)・HEAD=d5bdd76+PtyWin2ファイルmodのみ / smoke3代行実行: T1_QUICK exited code=0・T2_DRAIN(6MB)exited code=0＝**B2の0xC0000409解消を実証**（B3核心）・全孤児0・T3_WMCLOSEは非対話でMainWindowHandle=0のためkillフォールバック（graceful経路はT1/T2自主退出=同一QML close→Session close→kill→closePtyチェーンで被覆） / 不審検査全クリア（難読化/ネットワーク/認証情報/hooks/~接触なし、追加は термios no-opコメントのみ）/ **非ブロッキング新発見（B1領域・B3外・証拠付き）**: アプリstdoutを明示リダイレクトすると子cmdの生出力がアプリ継承stdoutハンドルへ到達（leaktest: 6MB type→leak-out.txt 6,008,895B / echo→18B生"HELLO_LEAK_PROBE\r\n"＝conhost VTレンダリング無し＝アプリのエコーではなく子の継承ハンドル直書き、ソースgrepでも端末データのstdout書込経路ゼロで裏付け）。**GUI subsystem実行体のため通常起動ではstdoutハンドル不在＝実運用不可視・無害**。B2監査の「6MBがSSHコンソール流入」メモの正体もこれ（監査役がb2-stdout.txt=6,008,895B成果物を閲覧）。ハードニング案（modifierでSTARTF_USESTDHANDLESクリア/hStd*をnull明示、または継承先未読pipe時のblock回避）はB1/B4領域で任意対応、plan.mdリスク節に追記。status を done に / CI・実機待ち: commit/push後のwindows CI緑（メイン確認、B1/B2前例どおり）+ 物理ログオンでのドラッグリサイズ・mode con追従・タブ×閉じ孤児ゼロ・split ランダムclose・3秒以内close（メイン Opus 代行監査）
- 2026-07-20 / B3 / CI緑確認（メイン）: d51e943（B3実装+plan.mdリスク追記+帳簿）を push → run 29713231463 success（conclusion=success・headSha=d51e943・4ジョブ緑）。B3 監査の残存確認事項「windows CI 緑」を充足 = B3 完全クローズ（PtyWin ConPTY バックエンド実装完了）。残りは物理ログオン目視のみ Phase B ゲートへ持ち越し
- 2026-07-20 / B4 / WIN32 専用 deploy_windows ターゲットと Windows CI の Deploy→15秒 Smoke→artifact v4 Upload を追加 / macOS x86_64 configure・Ruby Psych YAMLパース・Windowsステップ順と upload-artifact@v4 の静的検証・git diff --check は成功、macOSターゲット一覧に deploy_windows なし / 未実施: CI windows/ubuntu/macos 緑、artifact の Qt 未導入Windows実機起動、plan.md フルチェックリスト（オーケストレータ・ユーザー検証待ち）
- 2026-07-20 / B4 / 監査PASS（代行: メイン Opus）: 実装diff=app/CMakeLists.txt +11（if(WIN32)節内のみ・deploy_windowsカスタムターゲット、find_program windeployqt HINTS Qt6_DIR/../../../bin、exe向け--qmldir×2〈app/qml + qmltermwidget/src〉+ plugin dll向け--dir）・build.yml +17（build-windowsジョブのCheck outputs後にDeploy→15秒Smoke→upload-artifact@v4、ubuntu/macos無変更をdiffで確認）・両者タスクスニペット逐語 / mac configure exit 0・deploy_windowsはif(WIN32)内でmacターゲット一覧に不出現＝mac無影響 / **実機 deploy+自足性実証（オーケストレータ代行、この監査の核心）**: 箱を d51e943 同期+B4 CMake載せ→reconfigure+build+`cmake --build build --target deploy_windows`＝DEPLOY_OK。ステージ結果: exe隣にQt6Core/Gui/Quick/Qml/Sql/OpenGL.dll+platforms/qwindows.dll+sqldrivers/+root DLL 40本、QMLモジュールは build\qml\ 配下（qml\Qt5Compat\GraphicalEffects・qml\QtQuick\Controls 実在）/ **自足性の決定実験**: PATH_HAS_QT=False・QML2_IMPORT_PATH/QML_IMPORT_PATH/QT_PLUGIN_PATH全空の状態で、ステージ物を隔離コピー(C:\crt-iso)して起動→BASELINE alive_12s+子conhost/cmd出現＝Qt無しPATHで完全動作、さらに**staged qml\ を隠すとexit code 1(main.cpp EXIT_FAILURE=Cannot load QML interface、クラッシュにあらず)**＝QMLは C:\Qt ではなくステージ物から解決している証明（C:\Qt\qmlが副次import pathに居れば隠しても生存したはずだが終了＝QLibraryInfoはC:\Qt非参照）。qt.conf不在だがQt6のexe相対解決で挙動上無害と確定＝Phase Cクリーン Win10 VMゲートの前倒し実証。plugin(qwindows)もQML段到達＝exeDir先行解決で自足 / 不審検査全クリア（deploy targetはwindeployqt2呼びのみ・ネットワーク/難読化/hooks/認証情報なし・upload先はGitHub純正artifact）。status を done に / CI待ち: commit/push後の windows job（deploy+smoke新ステップの実CI通過が本タスクの新規ゲート）+ ubuntu/macos緑（B1〜B3前例どおりメイン確認）。ユーザー物理ログオン待ち: plan.md「検証」節フルチェックリスト（ssh+vim/htop・split・全シェーダープロファイル・設定永続化・単一インスタンス）（メイン Opus 代行監査）
- 2026-07-20 / B4 / CI緑確認（メイン）【Phase B 自動ゲート通過】: d8e3a5c を push → run 29713682588 success（4ジョブ全緑: Windows(MSVC)/macOS/Ubuntu 22.04/24.04）。**Windows ジョブで新規 Deploy Qt runtime→15秒 Smoke→artifact upload ステップが実 runner で通過**＝deploy_windows ターゲットと windeployqt HINTS 解決が CI 環境（install-qt-action の Qt）でも成立。B1〜B4 全て done+CI緑＝**Phase B の実装・自動検証は完了**。残る Phase B ゲート条件「cmd.exe が CRT シェーダー越しに動く」の目視のみユーザー物理ログオン待ち（実機 exe: C:\crt\src\build\cool-retro-term.exe、artifact: run 29713682588 の cool-retro-term-windows-staging）。次は Phase C（C1 zip=B4依存解放済/C2 README=無依存/C3 tree-kill=B3依存解放済）
- 2026-07-20 / C1 / release.yml に MSVC・Qt 6.10.* の Windows ZIP ジョブとリリースへの artifact 配線を追加し、package-windows.ps1 で build 直下の exe・全 DLL・denylist 外の全サブディレクトリを収集 / Ruby Psych YAML パース・workflow 配線・収集構造・git diff --check は成功、既存 AppImage/DMG ジョブはバイト一致、未実施: Windows CI・タグ/rolling release・クリーン VM での ZIP 展開/自足起動・アイコン表示（オーケストレータ/ユーザー確認待ち）、status を review に更新
- 2026-07-20 / C1 / 監査PASS（代行: メイン Opus）: 実装diff=release.yml +45（build-windowsジョブ新設: A8/B4のwindows setup逐語〈checkout submodules/install-qt-action 6.10.* win64_msvc2022_64 qt5compat+qtshadertools/msvc-dev-cmd/Ninja Release configure+build/deploy_windows〉+Package〈pwsh ./scripts/package-windows.ps1〉+upload-artifact@v4 cool-retro-term-win64、releaseジョブは needs に build-windows 追加+Download Windows ZIPステップ追加のみ、既存softprops files:./release/*がzip自動添付でpublish不変更・appimage/dmg/publish各ステップは1バイト不変）・新規 scripts/package-windows.ps1（denylist 11項目方式=exe+全ルート*.dll+非junkサブディレクトリ再帰収集、$ErrorActionPreference=Stop・stage毎回再作成・Compress-Archive）/ YAML妥当性ruby Psych OK / **実機で成果物ZIP自体を検証（この監査の核心、B4と同一の隔離手法）**: 箱の実deployツリーに対しpackage-windows.ps1実行→crt-win64.zip生成（105,527,590B）。**別ディレクトリC:\crt-zipへExpand-Archive展開しPATH_HAS_QT=False・QML/plugin系env空で起動→ZIP_BASELINE alive_12s+子conhost/cmd出現**＝クリーン相当環境で自足起動・cmd.exe動作（完了条件[2]充足）。決定実験: 展開物のqml\を隠すとexit code 1（Cannot load QML interface、クラッシュにあらず）＝ZIP同梱qmlから解決の証明。同梱物実在確認: cool-retro-term.exe/Qt6Core・Quick・Sql.dll/**platforms\qwindows.dll**（←タスク元whitelistなら欠落してクリーンVM起動不能だったplatformプラグイン、denylist方式で救済）/sqldrivers/qml\QtQuick\Controls/qml\Qt5Compat\GraphicalEffects/qmltermwidget\QMLTermWidget / 不審検査全クリア（ネットワーク/難読化/hooks/認証情報なし、upload先GitHub純正artifact、softprops既存流用） / メモ: ZIP 105MBはwindeployqt既定（exe向け呼びに--no-translations無しでtranslations同梱、qmltermwidget\配下のCMakeFiles junkも含む）由来。実害なし・将来のサイズ削減は任意。status を done に / **未実行（意図的）**: release.yml の実CI実行は release ワークフローが main/master/tag/workflow_dispatch トリガーのみで windows-port push では回らない。かつ workflow_dispatch を windows-port で叩くと release ジョブの publish が `if startsWith(refs/heads/)` で真になり feature ブランチから rolling を強制公開＋rollingタグforce-pushしてしまうため実行しない。実CI通過は master マージ時かユーザーの意図的タグ打ちで初回検証（構造は既存appimage/dmgジョブと同型で低リスク）。ユーザー物理ログオン待ち: Explorer/タスクバーの crt.ico 表示（A7で埋め込み済・ICO構造監査済）（メイン Opus 代行監査）
- 2026-07-20 / C2 / macOS は従来の Meta+Shift+D、非 macOS は Ctrl+Shift+F12 となるデバッグショートカット分岐と Windows の実績済み build/deploy 手順を README に追加 / `touch app/resources.qrc` 後の macOS x86_64 configure・build は exit 0（Automatic RCC 実行）、offscreen 起動は10秒以上生存し PaneTreeNode 生成まで成功・appSettings binding エラーなし、ネイティブ GUI 起動と実キー入力は実行環境の PasteBoard/HIS 接続制約で未確認、git diff --check 成功
- 2026-07-20 / C2 / 監査PASS（代行: メイン Opus）: 実装diff=PaneTreeNode.qml 1行（Shortcut sequence を appSettings.isMacOS ? "Meta+Shift+D" : "Ctrl+Shift+F12"）+README.md +29行（Windows節）+TASKS.md帳簿。スコープ内3ファイルのみ、resources.qrc は touch のみ内容diff無し / **appSettings.isMacOS 解決の裏取り**: isMacOS は ApplicationSettings.qml:41 実在（property bool isMacOS: Qt.platform.os==="osx"）・appSettings は main.qml:27 の root プロパティで BurnInEffect/PreprocessedTerminal/ShaderTerminal/TerminalTabs/TerminalContainer 等が別ファイルから bare 参照して動作＝QMLコンテキスト継承で全域解決、PaneTreeNode は TerminalTabs（自身 bare appSettings 使用）が生成する系譜でリペアレント（QQmlContext不変）非影響 / POSIX 無退行: mac は三項の true 側で Meta+Shift+D 従来どおり、非mac のみ Win キー衝突回避の Ctrl+Shift+F12 / README 事実性: Win10 1809+/Qt6.10+qt5compat+qtshadertools/MSVC2022/Ninja single-config必須警告/deploy_windows ターゲット/GUI subsystem の --help 非出力/COMSPEC 既定シェル/chcp 65001 は全て本セッションの実証事実・A6/A7/plan.md リスクと一致（絵空事なし）/ 独立検証: touch app/resources.qrc→mac x86_64 configure+build exit 0（Automatic RCC 実行確認）・offscreen 12秒起動で appSettings/ReferenceError/PaneTreeNode エラーの log 出力ゼロ＝binding 実行時解決を実証 / 不審検査全クリア（難読化/ネットワーク/hooks/認証情報なし・README は事実記述のみ）。status を done に / ユーザー物理ログオン待ち: ネイティブ GUI での実キー入力（mac ⌘⇧D / Windows Ctrl+Shift+F12 でスプリットデバッグダンプ）は HIS 接続制約で headless 不能、三項分岐と binding 解決は確認済み（メイン Opus 代行監査）
- 2026-07-20 / C3 / Job Object の生成・KILL_ON_JOB_CLOSE 設定・子プロセス割当・closePty末尾での破棄を純追加 / macOS x86_64 configure・build と git diff --check は成功（exit 0）、B3 終了順序・dtor 経路・二重 close の冪等性を静的確認 / 未実施: Windows 実機での孫プロセス tree-kill・B3 挙動非劣化、および CI 緑
- 2026-07-20 / C2 / CI緑確認（メイン）: c909c88 を push → run 29714788903 success（QML再コンパイル込み4ジョブ全緑）。C2 完全クローズ
- 2026-07-20 / C3 / 監査PASS（代行: メイン Opus）【全タスク完了】: 実装diff=PtyWin.cpp +18（start内 Job生成 + closePty末尾 job close）・PtyWin.h +1（HANDLE m_hJob=nullptr）のみ、純加算でB3確定の終了順序（writer停止→m_inWrite→ClosePseudoConsole→m_reader wait→m_outRead→attr list）は1行も並べ替えなし・job closeはその後に追加 / 契約実査: CreateJobObjectW→SetInformationJobObject(JOBOBJECT_EXTENDED_LIMIT_INFORMATION, KILL_ON_JOB_CLOSE)→OpenProcess(SET_QUOTA|TERMINATE)→AssignProcessToJobObject の標準パターン・spawn直後アサインの微小レースはコメント明記でnode-pty準拠・start失敗パスはjob生成が waitForStarted 成功後のため closePty 時 m_hJob=nullptr で全ガードスキップ・冪等性OK（2回目 m_hJob=nullptr skip）・dtor経路も closePty 通過でjob close / mac無退行: PtyWin はWIN32専用TUで mac build [100%] exit 0（独立確認）/ **実機決定実験（jobtest.ps1）**: -e cmd /c grandchild.cmd（start /b ping -n 999＝デタッチ孫 + foreground ping -n 60でcmd延命）→ BASELINE_PING=0→**DURING_PING=2**（孫ping2本稼働・ppid=cmd・APP_ALIVE=True）→アプリforce-kill→**AFTER_KILL_PING=0**＝アプリのハンドル閉→job最終ハンドル閉→KILL_ON_JOB_CLOSEでデタッチ孫まで全滅（B3なら孤児として残存＝差分実証、AFTER計測はcleanup前で純粋）/ B3無退行: -e cmd /c echo で SELFEXIT_CODE=0（0xC0000409非再発）・STRAY_APP=0・CHILD_OF_APP_PID=0（graceful closePtyのjob close追加でも clean 退出、先の STRAY_CMD=2 は SSH セッション自身の cmd/conhost 誤カウントと判明）/ 不審検査全クリア（純Win32 API・難読化/ネットワーク/hooks/認証情報なし）。status を done に＝**A1〜C3 全15タスク完了** / CI待ち: commit/push後の windows CI（PtyWin再コンパイル）。ユーザー物理ログオン待ち: タブ×閉じでの孫tree-kill目視（メイン Opus 代行監査）
- 2026-07-20 / C3 / CI緑確認（メイン）: 155392c を push → run 29715177493 success（4ジョブ全緑・Windows(MSVC) が Job Object 込みで PtyWin 再コンパイル通過）。**全15タスク実装・実機検証・監査・CI緑まで完了**
- 2026-07-20 / 【マイルストーン】Windows 移植 全タスク完了（メイン Opus / 2026-07-20 の 5h→6h ループ内で B1〜C3 を一気通貫）: Phase A（既完了）に続き B1〜B4（ConPTY spawn/reader-writer/resize+shutdown/windeployqt）+ C1〜C3（release zip/README+shortcut/Job Object tree-kill）を実装→実機SSH検証→監査→コミット→CI緑。コミット列 d5bdd76(B1)→5a04fee(B2)→d51e943(B3)→d8e3a5c(B4)→fbd5408(C1)→c909c88(C2)→155392c(C3)、全て origin/windows-port にpush済・各CI緑。実機実証の要点: cmd.exe spawn / 6MB排水 / 0xC0000409解消・exit 0 / Qt未導入相当で自足起動（qml隠し決定テスト）/ zip展開自足起動 / 孫プロセスtree-kill。**残りは①ユーザー物理ログオンでのGUI目視（CRTシェーダー越し表示・タイプ・Ctrl+C・ドラッグリサイズ・split・設定永続化・単一インスタンス。実機exe=C:\crt\src\build\cool-retro-term.exe、CI artifact=cool-retro-term-windows-staging）②release.ymlの実行（masterマージ or タグ打ちで初回、feature branchからのrolling誤公開回避のため未実行）③draft PR #5 の ready 昇格判断。いずれもユーザー判断待ち。自動化可能な実装作業は残ゼロ**
- 2026-07-21 / D1 / 実機描画バグ起票（メイン Opus + ユーザー実機観測）: Phase A〜C 完了後の実機物理ログオン検証で、端末内容が一切描画されないバグを発見。切り分けで PTY/エミュ（内容コピー可）・フォント（DirectWrite で等幅実測）・スクリーン/DPI（dpr=1・3440x1440 健全）・描画スロットル（frameDriver 強制連続でも出ず）・低解像度パス（非lowResでも同症状）を全て除外。有力仮説=QQuickPaintedItem→ShaderEffectSource キャプチャが D3D11 で空。副次バグ=terminalSize 桁/行入替。**教訓: 過去の実機スモークは全てプロセス生存/子プロセス確認どまりで GUI 描画を誰も目視していなかった（SSH セッション0はヘッドレスで描画ループ停止のため観測不能）**。観測はユーザー物理ディスプレイのみ（shot.ps1 スクショ＋fileIO ダンプ）。タスクファイル windows-plan/tasks/D1-crt-render-windows.md 作成、status を in-progress、Codex 調査へ委任
- 2026-07-21 / D1 / 【主症状解決・監査PASS(メインOpus)】Codex 調査で真因特定→修正→実機確定: terminal_dynamic.vert/frag の sampler が binding 0（Qt が UBO 予約）と衝突し、D3D11 用 HLSL で noiseSource が register t2863311530(=0xAAAAAAAA ゴミ)に化けて全テクスチャ束縛が失敗＝端末が黒。static shader は t0/t1 で正常だったため対比で確定。切り分け実験（Windows のみ CRT 迂回で生 kterminal 直接描画: PreprocessedTerminal.qml hideSource + ShaderTerminal.qml Loader.active を Qt.platform.os!=="windows" ガード）→ 実機で生端末＋橙カーソル表示＝QQuickPaintedItem 描画は正常・犯人はシェーダーと確定。**本修正**: 4サンプラーを binding 1..4 へ振り直し（noise=1/screenBuffer=2/burnIn=3/frame=4、UBO=0 据置、シェーダーロジック不変）+ compile_shaders で .qsb 再生成。実験は巻き戻し。検証: 再生成 .qsb から 2863311530 消滅（ローカル+実機ビルド両方 grep 0件）・mac x86_64 compile_shaders+build exit 0・mac offscreen 起動でシェーダーエラー0・**Windows 実機で CRT 越しに cmd 描画・echo エコー・flicker 反映・ウィンドウ一杯を実機目視確認（ユーザー）**。バックエンド安全性: binding 1+ は Qt6 正規契約、GL/Metal は 0 衝突を偶然許容していただけ、再生成 MSL は buffer(0)+texture(0-3)・GLSL sampler 名保持。**残り副次バグ（D1 継続・別レイヤ）**: ①Backspace が全削除（入力/キーボード or terminalSize 桁行入替の疑い、実機で再現継続）②カーソル点滅しない（描画スロットルが時間駆動の小再描画を間引く疑い）。status は in-progress のまま（①②解決で done）
- 2026-07-21 / D1 / 【副次バグ①Backspace 解決・監査PASS(メインOpus)】Codex 調査で真因特定→修正→実機確定: Windows 実機 cmd で Backspace が行全体を消す症状。真因=default.keytab の plain Backspace が 0x08(BS) を送るが、Windows ConPTY の VT 入力仕様では plain Backspace=0x7f(DEL) で、0x08 は Ctrl+Backspace(単語削除)として解釈される（MS Console VT docs 準拠）→ cmd が単語/行を消す。修正=Vt102Emulation.cpp:1105 に #if defined(Q_OS_WIN) ガードで plain Backspace(NoModifier)のみ送信バイトを 0x7f に変換（8+/1-、1ファイル）。POSIX 経路はバイト単位で不変（#if 除去で元と等価）、mac/Linux は 0x08 のまま。検証: mac x86_64 build exit 0（[100%]）・**Windows 実機で Backspace が1文字削除になったことを目視確認（ユーザー）**・keytab/QML/PtyWin/termios 不変更。残り副次バグ: ②カーソル点滅しない（描画スロットル疑い、継続）。status は in-progress のまま
- 2026-07-21 / D1 / 【副次バグ②カーソル点滅 解決・監査PASS(メインOpus)】真因: TerminalDisplay::updateCursor() が update(cursorRect) の部分更新で、Windows D3D11 RHI では painted item の部分更新が CRT ShaderEffectSource の再キャプチャを誘発しないため点滅が CRT を通らず不可視（内容更新=全体 update() は再キャプチャされるので echo/カーソル移動は出る）。修正: TerminalDisplay.cpp:2033 updateCursor に #if defined(Q_OS_WIN) で full update() 分岐（POSIX は従来どおり部分更新で効率維持）。検証: mac x86_64 build exit 0・実機で**カーソル点滅を目視確認（ユーザー）**。残り: 左上(0,0)に点滅するゴースト様アーティファクト（生端末モードでも観測＝CRT/本修正と無関係の端末描画側別件、調査継続）。status は in-progress のまま
- 2026-07-21 / D1 / 【全症状解決・D1 done】boot overlay (0,0) ゴースト修正で D1 完了: 起動オーバーレイ（(0,0) に点滅する █、シェル初出力で消える設計）が Windows で消えず点滅し続けていた。診断 stderr に「QML Connections: Cannot assign to non-existent property "enabled"」が該当 Connections 行で出ており、`enabled: bootOverlay.visible` ゲートで Connections/onReceivedData ハンドラが Windows で確立していなかった疑い。修正=PreprocessedTerminal.qml の bootOverlay Connections から enabled ゲートを外す（本命）+ 消え損ね安全網の Timer(1500ms) 追加。検証: mac x86_64 build exit 0・**実機で (0,0) ゴースト消滅を目視確認（ユーザー）**。**D1 全4症状解決**: ①CRT黒画面(シェーダーbinding, 3e6ca5d) ②Backspace全削除(0x7f, 08b7fa0) ③カーソル非点滅(全体update, 7819a8e) ④(0,0)boot overlayゴースト。実機で描画・タイプ・echo・Backspace・カーソル点滅・flicker 全て正常動作を確認。status を done に。**残る既知の軽微事項（別件・低優先、未起票）**: terminalSize QSize が桁/行入替で報告される（SizeOverlay 表示のみの cosmetic、端末実挙動は正常）。次セッションの候補: この cosmetic 修正、release.yml 実行（master マージ/タグ）、draft PR #5 ready 昇格
- 2026-07-21 / D2 / 起票（メイン Opus + ユーザー実機観測）: Windows 実機の WSL で ①ペースト時に bracketed paste マーカー ^[[200~/^[[201~ がリテラル混入 ②コピー/ペースト(Ctrl+Shift+C/V)不動作 ③Ctrl+U 不動作。初期調査: bracketText(TerminalDisplay.cpp:3104-3110) が bracketedPasteMode() true 時に囲む→WSL readline が剥がしていない＝モード状態不整合の疑い（Vt102Emulation の ?2004h/l 処理を要確認）。copy/paste は shortcuts.json(既定 Ctrl+Shift+C/V)+TerminalWindow.qml:92-96 のショートカット読込+qtermwidget copyClipboard/pasteClipboard 経路。Ctrl+U は D1 Backspace と同じ入力経路(Vt102Emulation::sendKeyEvent, 0x15 送信)。観測はユーザー物理ディスプレイのみ(shot.ps1/fileIO ダンプ)。タスクファイル windows-plan/tasks/D2-wsl-paste-input.md 作成、Codex 調査へ委任
- 2026-07-21 / D2 / 3経路を静的追跡し、Windows限定の一時診断を追加（未commit）: `C:\crt-setup\d2-input-trace.txt` に ?2004h/l→display mode、ペースト直前の mode/disable、copy/paste Action→QClipboard、C/V/U の Qt key/modifier→変換後バイト、ConPTY投入時の bracket marker/0x15 を本文非記録で採取。status は in-progress のまま、実機1往復の結果待ち
- 2026-07-21 / D2 / 【次セッション引き継ぎ・メイン Opus】Codex 調査完了、恒久修正は保留（実機ログ待ち）。**仮説ランキング（Codex）**: ①bracketed paste マーカー混入=「端末 mode と貼付時に読む readline 文脈の不一致 or mode 取り残し」が最有力（?2004h/l は Vt102→Emulation cache→TerminalDisplay へ正しく同期する構造で Windows 固有分岐なし・_disabledBracketedPasteMode は false 固定で有効化呼出なし）。②Ctrl+Shift+C/V 不動作=「QML Action の shortcut dispatch or 物理 Command/Win キーと Ctrl 取り違え」最有力（D1 で端末内容コピーは出来たので QClipboard 全体故障は低め）。③Ctrl+U 不動作=「実 modifier が Ctrl でなく Meta/Windows」最有力（ユーザー表現「Command+U」と整合、mac 用 remap は Windows で無効。keytab に U/C/V 明示 entry なし・汎用変換で 0x55&0x1f=0x15、2つの default.keytab はバイト一致）。**診断（未コミット・working tree に温存）**: Windows 限定 D2InputTrace ＝ 変更5ファイル（qmltermwidget/lib/D2InputTrace.h〈新規〉+ TerminalWindow.qml + TerminalDisplay.cpp + Vt102Emulation.cpp + PtyWin.cpp）。C:\crt-setup\d2-input-trace.txt に長さ/フラグ/制御バイトのみ記録（本文非保存）: ?2004h/l→display mode 遷移、ペースト直前の mode/disabled/willWrap、QClipboard 読出長・copy 選択長/書込後 clipboard 長、実ロード copy/paste shortcut と Action 発火、C/V/U の Qt key/modifier/native/変換後 outputHex、ConPTY 投入直前の marker 有無・単独 0x15 有無。**次セッション実機1往復手順**: ①box を D2 診断で同期（git reset --hard origin/windows-port 後に上記5ファイルを scp）→ C:\crt\src\app\qml\resources.qrc を touch → C:\crt-setup\build.cmd（アプリ kill 後）②旧 d2-input-trace.txt 削除して起動③WSL bash で `bind -v | grep enable-bracketed-paste` 確認④既知文字列を Edit→Paste と Ctrl+Shift+V の両方で貼付、選択を Edit→Copy と Ctrl+Shift+C でコピー、Ctrl+U 押下⑤d2-input-trace.txt を scp 回収して解析→機序確定→恒久修正を Codex へ（codex-companion --resume-last か codex resume で D2 セッション継続）。診断は恒久修正確定後に revert。status は in-progress のまま
